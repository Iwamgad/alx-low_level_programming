#include "main.h"

/**
 * set_string - sets the value of a pointer to a char
 * @s: a pointer to an character pointer
 * @to: a character pointer
 * Return: returns nothing
 */

void set_string(char **s, char *to)
{
	*s = to;
}
